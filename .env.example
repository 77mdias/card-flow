APP_BASE_URL="http://localhost:3000"
BETTER_AUTH_SECRET="use-openssl-rand-hex-32"

# Runtime (aplicacao): prefira endpoint pooler no Neon
DATABASE_URL="postgresql://USER:PASSWORD@HOST-POOLER:5432/DATABASE?sslmode=require&channel_binding=require&connect_timeout=15"

# Prisma migrate/studio/status: prefira endpoint direto (sem pooler)
DIRECT_URL="postgresql://USER:PASSWORD@HOST-DIRECT:5432/DATABASE?sslmode=require&connect_timeout=15"

# SMTP para envio via Nodemailer (verificacao de email do Better Auth)
SMTP_HOST="smtp.provider.com"
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER="smtp-username"
SMTP_PASS="smtp-password"
SMTP_FROM="CardFlow <noreply@77code.com.br>"
SMTP_REPLY_TO="suporte@77code.com.br"
